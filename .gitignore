# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# testing
/coverage

# browser automation artifacts (screenshots, console/network logs)
/.playwright-mcp/

# misc
.DS_Store
*.pem

# local design reference (MSB app screenshots — not part of the codebase)
/ref-ui-msb/

# local competitor benchmark reference (BIDV/TIMO screenshots — not part of the codebase)
/ref/

# local planning workspace (not part of the codebase)
/plans/
/todo.md

# local reference docs for the real Agent backend (not part of the codebase)
/agent_backend_docs/

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# SQLite journal sidecar files (data/pfm.sqlite3 itself IS committed — only
# these ephemeral WAL-mode files, present while a dev server holds an open
# connection, are excluded)
data/*.sqlite3-shm
data/*.sqlite3-wal

