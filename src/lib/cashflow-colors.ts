/**
 * Expense series color for the Chi tiêu charts, on the MSB theme: Chi tiêu uses
 * the MSB brand orange (`--color-primary`). Kept in one place so the bars (Tổng
 * quan) and the lines (Biến động) stay identical. Income was removed.
 */

export const EXPENSE_COLOR = "#f26522"; // Chi tiêu — MSB brand cam

/** Vertical gradient stops (top → bottom) for the solid bars. */
export const EXPENSE_GRADIENT = { from: "#f5822c", to: "#e8321e" };
